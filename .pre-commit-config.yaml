repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.9
    hooks:
      - id: ruff
        args: [--fix]
        files: ^(prism|tests|scripts|train\.py)$
      - id: ruff-format
        files: ^(prism|tests|scripts|train\.py)$
